<article class="article article-template" itemscope itemtype="http://schema.org/BlogPosting" {% if article.metafields.custom.type != blank %}data-type="{{ article.metafields.custom.type }}"{% endif %}>
  {%- for block in section.blocks -%}
    {%- case block.type -%}

        {%- when 'content'-%}
          <div class="article-content" itemprop="articleBody" {{ block.shopify_attributes }}>
            <div class="container-fluid p-0">
              <div class="row">
                {{ article.content }}
              </div>
            </div>
          </div>

    {%- endcase -%}
  {%- endfor -%}
</article>

{% schema %}
{
  "name": "(ATK) Main Article",
  "tag": "section",
  "class": "section",
  "blocks": [
    {
      "type": "content",
      "name": "(ATK) Main Article",
      "limit": 1
    }
  ]
}
{% endschema %}
